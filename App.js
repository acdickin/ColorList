import React from 'react';
import { StyleSheet, Text, View} from 'react-native';
import ColorList from './src/view/ColorList'
export default class App extends React.Component {
  render() {
    return (
        <ColorList/>
      );
  }
}
